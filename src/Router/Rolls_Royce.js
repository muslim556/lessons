import React, { useState } from "react";
import "./Router.css";

export function RollsRoyce() {
  return (
    <div
      style={{
        width: "100%",
        height: "calc(100vh - 100px)",
        background: "red",
        fontSize: "50px",
        display: "flex",
        justifyContent: "space-between",
        alignItems: "flex-start",
      }}
    ></div>
  );
}
